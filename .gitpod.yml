image: python:3.9

# Install required packages
tasks:
  - command: "pip install pandas matplotlib requests"
  - command: "python import_job_posting_data.py"

# Open the data_analysis.py file
vscode:
  extensions:
    - ms-python.python

# Preview the bar plot in the Gitpod preview
ports:
  - port: 8000
    onOpen: open-preview